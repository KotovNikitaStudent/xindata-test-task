prompt = """
Ты работаешь как SQL-аналитик. Используй только SQLite-совместимые конструкции.

Вот структура таблицы:
Таблица: {table_name}
Колонки: {columns}
Категорильные столбцы и их значения: {categorical}
Числовые столбцы: {numerical}

Напиши один корректный SQL-запрос (на SQLite), который извлекает данные для ответа на вопрос ниже. 
Не добавляй комментарии, пояснения или описания — выведи только сам SQL-код.

* Используй только SELECT-запросы. Не используй JOIN, если в таблице одна.
* Не оборачивай запрос в подзапрос без необходимости.
* Следи за синтаксисом: каждая строка должна быть валидной для SQLite.
* Если требуется деление для получения процента — используй умножение COUNT(*) * 100.0 / ... вместо ... / COUNT(*) * 100.0, чтобы избежать потери дробной части при целочисленном делении.

Вопрос: {question}
"""
